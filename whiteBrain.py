from transition import Machine
import random

class whiteDragon(object)
    state = ['sleep', 'hanging out', 'hungry', 'sweaty', 'saving the world']

    def __init__(self, name):
        self.name = name
        self.cat_rescued = 0
        self.machine = Machine(model = self, states =whiteDragon.states,  )

















